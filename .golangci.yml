run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet
    - ineffassign
    - staticcheck
    - gofmt

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters: [govet, staticcheck]
